// Faça um programa em C que converta um valor qualquer em Gigabytes para um valor em bytes (1GB = 1024³Bytes).

#include <stdio.h>

int main()

{
printf("Digite o valor em gigabytes:");
scanf("%f", & gigabites);
gigabites = 1024.0;

printf("O valor em bytes e   %f", gigabites);

return 0;
}